# Legacy ENA settings (now stored in database via Site Settings)
# These are only used as fallback if not configured in Site Settings
ENA_USERNAME=
ENA_USER=
ENA_PASSWORD=

# Field encryption key for sensitive data
# Generate a new key for production using: 
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=

ROOT_DIR=$HOME/git/django_ngs_metadata_collection
USE_SLURM_FOR_SUBMG=False
CONDA_PATH=
